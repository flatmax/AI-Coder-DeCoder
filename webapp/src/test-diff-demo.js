/**
 * Test file for diff highlighting demo.
 * Delete this file before committing.
 */

// Example 1: Simple word change
const greeting = "Hello universe";
const farewell = "Goodbye world";

// Example 2: Multiple changes on one line
function computeSum(cost, amount) {
  return cost * amount;
}

// Example 3: Variable rename
const customerName = "Alice";
const customerAge = 25;

// Example 4: Import statement change
import { something } from './new-module.js';

// Example 5: Longer line with small change
const configOptions = { debug: true, verbose: false, maxRetries: 5 };

export { greeting, farewell, computeSum };
